description = "dao"

dependencies {
    compile project(':core')
    compile project(':domain')

    testCompile project(':core')
    testCompile project(':core').sourceSets.test.output
}

jar {
    baseName "sqlapi4j-dao"
}